export class FriendHandler {
    static C_ADD_FRIEND() {
        console.error('friend协议并不会Routing到relation server,这里是为了防止报错。');
    }

    static C_ADD_FRIEND_REQ_RESULT() {
        console.error('friend协议并不会Routing到relation server,这里是为了防止报错。');
    }
}